<UserControl x:Class="RamOptimizerUI.Views.ServiceTestingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="850">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Page Title -->
            <TextBlock Text="🧪 Service Testing (BETA)" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,30"/>
            
            <!-- Warning Card -->
            <Border Style="{StaticResource Card}" Background="#3d1e1e" BorderBrush="#f44336" BorderThickness="2" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="⚠️ WARNING: High-Risk Operation" FontSize="18" FontWeight="Bold" Foreground="#f44336" Margin="0,0,0,12"/>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8">
                        This feature will stop and restart Windows services to test system stability.
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8">
                        • A system restore point will be created before testing
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8">
                        • Your computer may automatically reboot if issues are detected
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextBrush}">
                        • Progress is saved every service - testing can resume after reboot
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Test Options Card -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Test Options" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <CheckBox x:Name="TestUserServicesCheckBox" Content="Test user services" IsChecked="True" Margin="0,0,0,8" Foreground="{StaticResource TextBrush}"/>
                    <CheckBox x:Name="TestSystemServicesCheckBox" Content="Test system services" IsChecked="True" Margin="0,0,0,8" Foreground="{StaticResource TextBrush}"/>
                    <CheckBox x:Name="SkipEssentialCheckBox" Content="Skip known essential services (Recommended)" IsChecked="True" Margin="0,0,0,16" Foreground="{StaticResource TextBrush}"/>
                    
                    <TextBlock x:Name="ServiceCountText" Text="Estimated services to test: Calculating..." FontSize="12" Foreground="{StaticResource SubtextBrush}" Margin="0,0,0,16"/>
                    
                    <UniformGrid Columns="4" Rows="1">
                        <Button x:Name="StartTestButton" Content="🚀 Start Testing" Style="{StaticResource SuccessButton}" Margin="0,0,8,0" Click="StartTest_Click"/>
                        <Button x:Name="StopTestButton" Content="🛑 Emergency Stop" Style="{StaticResource DangerButton}" Margin="0,0,8,0" Click="StopTest_Click" IsEnabled="False"/>
                        <Button x:Name="ViewBlacklistButton" Content="📋 View Blacklist" Style="{StaticResource ModernButton}" Margin="0,0,8,0" Click="ViewBlacklist_Click"/>
                        <Button x:Name="ClearCheckpointButton" Content="🔄 Reset Progress" Style="{StaticResource ModernButton}" Margin="0,0,0,0" Click="ClearCheckpoint_Click"/>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- Progress Card -->
            <Border Style="{StaticResource Card}" x:Name="ProgressCard" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Test Progress" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <TextBlock x:Name="CurrentServiceText" Text="" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="StatusText" Text="Ready to start..." FontSize="13" Foreground="{StaticResource SubtextBrush}" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ProgressBar x:Name="TestProgressBar" Grid.Column="0" Height="24" Minimum="0" Maximum="100" Value="0" Margin="0,0,10,0"/>
                        <TextBlock x:Name="ProgressPercentText" Grid.Column="1" Text="0%" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <TextBlock x:Name="ProgressDetailText" Text="0 / 0 services tested" FontSize="12" Foreground="{StaticResource SubtextBrush}"/>
                </StackPanel>
            </Border>

            <!-- System Health Card -->
            <Border Style="{StaticResource Card}" x:Name="HealthCard" Visibility="Collapsed" Background="#1e2a1e" BorderBrush="#4CAF50" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="📊 System Health Monitor" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock x:Name="NetworkStatusText" Text="Network: ✓ OK" FontSize="13" Foreground="#4CAF50" Margin="0,0,0,6"/>
                            <TextBlock x:Name="DiskStatusText" Text="Disk I/O: ✓ OK" FontSize="13" Foreground="#4CAF50" Margin="0,0,0,6"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="DisplayStatusText" Text="Display: ✓ OK" FontSize="13" Foreground="#4CAF50" Margin="0,0,0,6"/>
                            <TextBlock x:Name="CheckpointStatusText" Text="Last Checkpoint: None" FontSize="13" Foreground="{StaticResource SubtextBrush}" Margin="0,0,0,6"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Results Card -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Test Results" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Tested" FontSize="12" Foreground="{StaticResource SubtextBrush}"/>
                            <TextBlock x:Name="TestedCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="Problematic" FontSize="12" Foreground="{StaticResource SubtextBrush}"/>
                            <TextBlock x:Name="ProblematicCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#f44336"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Skipped" FontSize="12" Foreground="{StaticResource SubtextBrush}"/>
                            <TextBlock x:Name="SkippedCountText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#FFA500"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Results List -->
                    <Border BorderBrush="{StaticResource SubtextBrush}" BorderThickness="1" CornerRadius="4" MaxHeight="300">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ListBox x:Name="ResultsListBox" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="8" Margin="0,0,0,4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="30"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontSize="13" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                                <TextBlock Grid.Column="2" Text="{Binding ImpactSummary}" FontSize="11" Foreground="{StaticResource SubtextBrush}" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
