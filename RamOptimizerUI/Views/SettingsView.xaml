<UserControl x:Class="RamOptimizerUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Page Title -->
            <TextBlock Text="⚙️ Settings" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,30"/>
            
            <!-- General Settings Card -->
            <Border Style="{DynamicResource Card}">
                <StackPanel>
                    <TextBlock Text="General Settings" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>
                    
                    <!-- Startup Behavior -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Run on Windows Startup" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="Automatically start RamOptimus when you log in" FontSize="12" Foreground="{DynamicResource SubtextBrush}"/>
                        </StackPanel>
                        <CheckBox x:Name="RunOnStartupCheckBox" Grid.Column="1" VerticalAlignment="Center" 
                                  Checked="RunOnStartup_Checked" Unchecked="RunOnStartup_Unchecked">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                            </CheckBox.LayoutTransform>
                        </CheckBox>
                    </Grid>

                    <!-- ASUS Services -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Disable ASUS Services" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="Stop ASUS background services to prevent conflicts (Recommended)" FontSize="12" Foreground="{DynamicResource SubtextBrush}"/>
                        </StackPanel>
                        <CheckBox x:Name="DisableAsusServicesCheckBox" Grid.Column="1" VerticalAlignment="Center"
                                  Checked="DisableAsusServices_Checked" Unchecked="DisableAsusServices_Unchecked">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                            </CheckBox.LayoutTransform>
                        </CheckBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- About Card -->
            <Border Style="{DynamicResource Card}">
                <StackPanel>
                    <TextBlock Text="About RamOptimus" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Version:" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Width="100"/>
                        <TextBlock Text="2.0.0" Foreground="{DynamicResource SubtextBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Developer:" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Width="100"/>
                        <TextBlock Text="JRTWolf" Foreground="{DynamicResource SubtextBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="License:" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Width="100"/>
                        <TextBlock Text="Private Use" Foreground="{DynamicResource SubtextBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
